# ===========================================
# Frontend Configuration (Vite)
# ===========================================

# Backend server URL for API requests
# Development: http://localhost:3001/api
# Production: https://your-render-server.railway.app/api
VITE_API_BASE_URL=http://localhost:3001/api

# Socket.IO server URL for real-time communication (export progress, etc.)
# Development: http://localhost:3001
# Production: https://your-render-server.railway.app
VITE_SOCKET_URL=http://localhost:3001

# Enable/disable server rendering option in export dialog
# Set to 'false' to hide server tab (e.g., Vercel without render server)
VITE_ENABLE_SERVER_RENDER=true

# ===========================================
# Render Server Configuration (Node.js)
# ===========================================

# Server port (default: 3001)
PORT=3001

# Environment mode
NODE_ENV=development

# CORS allowed origins (comma-separated for multiple)
# Development: http://localhost:5173
# Production: https://your-app.vercel.app,https://your-custom-domain.com
CORS_ORIGIN=http://localhost:5173

# Host binding (0.0.0.0 for containers, 127.0.0.1 for local only)
HOST=0.0.0.0
