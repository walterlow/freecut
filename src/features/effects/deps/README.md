# effects/deps

Effects-local adapters for external feature dependencies.

- `timeline-contract.ts`: adapter exports for effects modules that need
  timeline stores/actions.
- `preview-contract.ts`: adapter exports for effects modules that need
  preview stores/hooks.
