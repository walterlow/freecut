# keyframes/deps

Keyframes-local adapters for external feature dependencies.

- `timeline.ts`: the preferred entry point for keyframes modules that need
  timeline stores.
- `preview-contract.ts`: adapter exports for keyframes modules that need
  preview hooks.
- `composition-runtime-contract.ts`: adapter exports for keyframes modules
  that need composition-runtime transform helpers.
