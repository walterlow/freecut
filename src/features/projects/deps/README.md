# projects/deps

Projects-local adapters for external feature dependencies.

- `settings-contract.ts`: adapter exports for projects modules that need
  settings store access.
- `media-library-contract.ts`: adapter exports for projects modules that need
  media-library services.
