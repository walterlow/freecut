/**
 * Contract: live-ai → editor. Single seam for cross-feature imports.
 */
export { useTimelineStore } from '@/features/editor/deps/timeline-store';
export { useProjectStore } from '@/features/editor/deps/projects';
