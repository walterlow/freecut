/**
 * Adapter exports for projects dependencies used by workspace-gate.
 * Workspace-gate modules must import projects stores through this file.
 */

export { useProjectStore } from '@/features/projects/stores/project-store'
