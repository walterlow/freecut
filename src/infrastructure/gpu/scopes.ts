/**
 * Infrastructure facade for GPU scopes (waveform, vectorscope, etc.).
 * All consumers should import scope utilities from here instead of @/lib/gpu-scopes.
 */

export { ScopeRenderer } from '@/lib/gpu-scopes';
