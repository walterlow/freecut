/**
 * Infrastructure facade for GPU mask rendering.
 * All consumers should import mask utilities from here instead of @/lib/masks/*.
 */

export { MaskTextureManager } from '@/lib/masks/mask-texture-manager';
